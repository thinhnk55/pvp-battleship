namespace Framework
{
    public enum GameEvent
    {
        LostConnection = 0,
        LUCKYSHOT_FIRE,
        SELECT_ACHIEVEMENT,
        SHIP_DESTROY,
        SHIP_DESTROY_INIT,
        SHIP_HIT,
        GAME_END,
        REWARD_ADS_INFO,
        LOG_OUT,
    }
}