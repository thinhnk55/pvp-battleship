using Framework;
using System.Collections;
using System.Collections.Generic;
using TMPro;
using Unity.VisualScripting;
using UnityEngine;
using UnityEngine.UI;

public class ShowRewardPopup : MonoBehaviour
{
    [SerializeField] Button forwardButton, backwardButton;
    [SerializeField] TextMeshProUGUI amountReward;
    [SerializeField] int rewardStartLevel;
    //private PDataUnit<int> leveNeedShowReward = new PDataUnit<int>(rewardStartLevel);
    // Start is called before the first frame update
    void Start()
    {
        
    }
    
    private void OnClickForwardButton()
    {
        
    }
}
